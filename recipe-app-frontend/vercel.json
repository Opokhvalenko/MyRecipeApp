{
  "version": 2,
  "buildCommand": "npm install && npx expo export",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/_expo/static/(.*)",
      "dest": "/dist/_expo/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/index.html"
    }
  ]
}
